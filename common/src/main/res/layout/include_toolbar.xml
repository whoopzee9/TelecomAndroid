<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/dp_56"
    android:background="@color/toolbar_background"
    android:elevation="@dimen/dp_4">

    <ImageButton
        android:id="@+id/include_toolbar__ib_button"
        style="@style/ImageButton.ToolBarActionButton"
        android:contentDescription="@string/description_back"
        android:src="@android:drawable/ic_delete"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/include_toolbar__tv_title"
        style="@style/TextView.Toolbar.Title"
        android:layout_width="@dimen/dp_0"
        android:layout_height="@dimen/dp_0"
        android:layout_margin="@dimen/dp_16"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/include_toolbar__ib_button"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Тулбар" />
</androidx.constraintlayout.widget.ConstraintLayout>